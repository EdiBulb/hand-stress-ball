# "다른 사람 ball 구경하기" 백엔드 구현 플랜 (첫 백엔드 스터디용)

지금까지 만든 Hand Stress Ball은 카메라도, 손 인식도, 소리도 전부 **브라우저 안에서만** 일어나는 일이야. 서버가 하나도 없어. 그런데 "내가 올린 ball을 다른 사람도 볼 수 있게" 하려는 순간, 처음으로 "여러 사람이 공유하는 뭔가"가 필요해져. 이게 왜 브라우저만으로는 안 되는지부터, 어떤 조각들을 어떤 순서로 만들면 되는지까지 최대한 쉽게 풀어볼게.

---

## 0. 왜 지금까지는 백엔드가 필요 없었을까

지금까지 만든 기능은 전부 **네 브라우저 안에서 혼자 일어나는 일**이었어. 카메라 영상도, 손 인식 결과도, "내 스트레스볼 추가하기"로 올린 사진도 전부 네 컴퓨터의 메모리 안에만 존재하고, 페이지를 새로고침하면 사라져. 다른 사람의 브라우저는 네 브라우저 안에서 무슨 일이 일어나는지 전혀 알 방법이 없어 — 완전히 분리된 각자의 방이라고 생각하면 돼.

"다른 사람이 만든 ball을 나도 갖고 논다"는 건, **네 방에 있는 물건을 남의 방에서도 볼 수 있어야 한다**는 뜻이야. 방과 방은 직접 연결되어 있지 않으니까, 모두가 접근할 수 있는 **공용 창고**가 하나 있어야 해. 그 공용 창고를 관리하고, "누가 뭘 넣었고 뭘 꺼내갈 수 있는지"를 정리해주는 존재가 바로 **백엔드(서버)**야.

---

## 1. 큰 그림: 세 가지 역할

전체 구조는 이렇게 세 부분으로 나뉘어.

**① 브라우저 (클라이언트)** — 지금 만든 React 앱. 업로드 폼, 갤러리 화면을 보여주는 부분. 지금까지 해온 것과 똑같아.

**② API 서버 (백엔드)** — 브라우저의 요청을 받아서 처리해주는 "창구 직원". 비유하자면 우체국 창구 같은 거야. 네가 소포(이미지)랑 송장(이름, 만든 사람)을 들고 가면, 창구 직원이 "이 소포는 창고에 넣고, 송장 내용은 장부에 적어둘게요" 하고 처리한 다음 "접수됐습니다" 하고 돌려보내줘.

**③ 저장소 (두 가지로 나뉨)**
- **오브젝트 스토리지** — 실제 이미지 파일(무거운 짐)을 보관하는 창고.
- **데이터베이스** — "누가, 언제, 어떤 이름으로, 어느 창고 칸에" 넣었는지 적어두는 장부/카탈로그.

### 왜 이미지 파일이랑 정보(이름, 제작자)를 따로 저장할까?

도서관을 생각해봐. 실제 책들은 서고(창고)에 빽빽하게 꽂혀 있고, 입구에 있는 카드 카탈로그(장부)에는 "책 제목, 저자, 몇 번째 서가"라는 짧은 정보만 적혀 있지. 책을 찾을 때 서고를 처음부터 다 뒤지지 않고, 가벼운 카탈로그부터 훑어보고 위치를 알아낸 다음 그 자리로 바로 가잖아.

똑같은 원리야. 이미지 파일(수백 KB~수 MB)을 매번 통째로 뒤지면서 목록을 만들면 느리고 비효율적이야. 그래서 이미지는 **오브젝트 스토리지**에 넣고 URL 하나만 받아서, 그 URL을 가벼운 정보(이름, 제작자, 업로드 시각)와 함께 **데이터베이스**에 적어두는 거야. 갤러리 화면을 보여줄 땐 데이터베이스만 빠르게 훑고, 실제 이미지는 그 URL로 필요할 때 불러와.

---

## 2. 이번에 쓸 도구들, 그리고 왜 그걸 쓰는지

지금 "Will You Hire Me" 프로젝트도 Vercel에 배포했었지? 이번에도 같은 플랫폼을 그대로 써서 배우는 도구 수를 최소화할게.

### Vercel Serverless Functions (API 서버 역할)
`api/` 폴더 안에 파일 하나(`api/balls.ts`처럼)를 만들면 그게 곧 API 엔드포인트가 돼. 서버를 직접 켜놓고 계속 관리할 필요가 없어 — 요청이 들어올 때만 잠깐 실행되고 끝나는 "온디맨드 창구 직원"이라고 생각하면 돼. Will You Hire Me 때 `api/oauth/start.ts`를 만들었던 것과 똑같은 방식이야.

### Vercel Blob (이미지 저장 창고)
이미지 파일을 업로드하면 URL 하나를 돌려주는 저장소야. Hobby(무료) 플랜 기준 **월 1GB 저장 + 10GB 전송량**까지 무료야. ([Vercel Blob 문서](https://vercel.com/docs/vercel-blob), [pricing](https://vercel.com/docs/vercel-blob/usage-and-pricing)) 단, 무료 플랜은 **수익이 발생하는 프로젝트에는 못 쓴다는 제한**이 있어 — 지금은 포트폴리오/브랜딩용이라 문제없지만, 나중에 유료화할 계획이 생기면 이 부분은 다시 확인해야 해.

### Redis (메타데이터 장부 역할)
원래는 "Vercel KV"라는 서비스를 추천하려고 했는데, 확인해보니 **Vercel KV는 없어졌고**, 지금은 Vercel Marketplace에서 연결하는 **Upstash Redis**로 대체됐어. ([Vercel Redis 문서](https://vercel.com/docs/redis)) 개념은 똑같아 — "키(key) 하나에 값(value) 하나"를 저장하는 아주 단순한 장부라고 생각하면 돼. 예를 들어 `ball:abc123` 이라는 키에 `{ name, creatorName, imageUrl, createdAt, approved }` 라는 값을 저장하는 식.

**왜 처음부터 진짜 SQL 데이터베이스(Postgres 같은)를 안 쓰냐면**, 지금 필요한 건 "목록에 추가하고, 목록을 통째로 꺼내오는" 정도의 단순한 작업이라 복잡한 검색/관계 설정이 필요 없어. Redis 같은 key-value 저장소가 설정도 간단하고 배우기도 훨씬 쉬워. 나중에 "인기순 정렬", "댓글", "좋아요" 같은 복잡한 기능이 생기면 그때 Postgres로 넘어가도 늦지 않아.

---

## 3. 업로드 흐름 (단계별로 쪼개보기)

1. 사용자가 "내 ball 등록하기"에서 사진을 선택해.
2. **브라우저 안에서 canvas로 정사각형으로 자르고 리사이즈** — 이건 지금 있는 업로드 미리보기 로직을 조금 확장하면 돼. 서버에 원본 그대로 보내지 않고 미리 규격을 맞춰서 보내면, 서버 부담도 줄고 갤러리에 표시될 때도 다 일정한 크기로 보여.
3. 이름, 만든 사람 이름을 입력해.
4. "등록" 버튼을 누르면 브라우저가 이미지 + 이름/제작자명을 `POST /api/balls`로 전송해.
5. 서버(API 함수)가 요청을 받아서 순서대로:
   - 진짜 이미지 파일이 맞는지, 용량이 너무 크지 않은지 확인 (예: 5MB 이하, `image/png`나 `image/jpeg`만 허용)
   - Vercel Blob에 이미지를 업로드하고 URL을 받음
   - Redis에 `{ id, name, creatorName, imageUrl, createdAt, approved: false }` 형태로 저장
   - "접수됐어요, 승인 후 갤러리에 올라갑니다" 라고 응답
6. 사용자 화면엔 "검토 후 갤러리에 표시됩니다" 라는 안내를 보여줘.

## 4. 조회(갤러리) 흐름

1. "다른 사람들이 만든 ball 구경하기" 화면에 들어가.
2. 브라우저가 `GET /api/balls?approved=true`로 요청해.
3. 서버가 Redis에서 승인된 것만 골라서 목록을 JSON으로 돌려줘.
4. 프론트엔드가 그 목록을 그리드로 그려주고, 하나를 클릭하면 그 ball의 `imageUrl`을 지금 있는 "커스텀 이미지" 경로(`imageUrl` prop) 그대로 재사용해서 손으로 쥐고 놀 수 있어. 즉, 갤러리 기능은 지금 있는 "내 사진으로 스트레스볼 만들기" 로직 위에 "저장하고 공유하는" 레이어만 얹는 거라, 완전히 새로 만드는 게 아니야.

## 5. 검수(모더레이션) — 왜 필요하고 어떻게 시작할까

누구나 이미지를 올릴 수 있는 공개 갤러리는 부적절한 이미지가 올라올 위험이 있어. 학교 게시판에 아무나 포스터를 바로 붙일 수 있게 하면 안 되고, 선생님이 한 번 보고 붙여주는 것처럼, **처음엔 사람이 직접 확인하는 방식**이 제일 안전하고 구현도 쉬워.

- 업로드된 항목은 기본값으로 `approved: false`
- 승인 여부를 바꾸는 별도의 관리자 화면/인증 로직 없이, **Upstash Redis 콘솔에서 직접 `approved`를 `true`로 바꿔주는 수동 방식부터 시작**해도 충분해. 사용자가 몇 명 안 되는 초기 단계엔 이게 제일 간단해.
- 나중에 사용자가 늘어나면, 신고 버튼이나 별도의 관리자 승인 페이지, 자동 이미지 검열 API 같은 걸 추가로 붙이면 돼. 지금 단계에서 미리 다 만들 필요는 없어.

---

## 6. 실제로 만들 때 추천하는 순서 (막막하지 않게 쪼갠 순서)

1. Vercel 대시보드에서 이 프로젝트에 **Blob**이랑 **Redis(Marketplace 통합)** 연결 — 클릭 몇 번이면 끝나.
2. 로컬 개발 환경 세팅: `vercel dev`로 실행하고, `.env.local`에 자동으로 채워지는 토큰들 확인.
3. **제일 작은 것부터**: `POST /api/balls` 엔드포인트 하나만 먼저 만들고, 프론트엔드는 건드리지 않은 채로 curl이나 Postman으로 직접 테스트해서 "이미지가 Blob에 잘 올라가고, Redis에 기록도 잘 되는지" 확인.
4. `GET /api/balls` 엔드포인트 만들어서 목록이 잘 나오는지 테스트.
5. 프론트엔드의 업로드 폼을 실제 API 호출로 연결.
6. 갤러리 화면 컴포넌트 만들어서 `GET /api/balls` 붙이기.
7. Redis 콘솔에서 `approved`를 수동으로 바꿔보면서, "승인 전엔 안 보이고 승인 후엔 보이는지" 전체 흐름 테스트.
8. 배포하고, 실제로 이미지 하나 올려서 눈으로 확인.

이 순서의 핵심은 **서버 쪽을 먼저 완성하고 눈으로 확인한 다음에 프론트엔드를 연결하는 것**이야. 두 개를 동시에 만들면서 디버깅하면 어디서 문제가 생겼는지 찾기 훨씬 어려워져.

---

## 7. 감 잡기용 코드 스니펫 (전체 구현 아님, 형태만)

### 업로드 API (`api/balls.ts`)의 대략적인 모양

```ts
import { put } from '@vercel/blob'
import { Redis } from '@upstash/redis'

const redis = Redis.fromEnv()

export default async function handler(req: Request) {
  if (req.method !== 'POST') return new Response('Method not allowed', { status: 405 })

  const formData = await req.formData()
  const file = formData.get('image') as File
  const name = formData.get('name') as string
  const creatorName = formData.get('creatorName') as string

  // 1) 검증: 용량, 타입 체크는 여기서
  if (file.size > 5 * 1024 * 1024) {
    return new Response('파일이 너무 커요', { status: 400 })
  }

  // 2) 이미지를 창고(Blob)에 업로드
  const blob = await put(`balls/${crypto.randomUUID()}.png`, file, { access: 'public' })

  // 3) 장부(Redis)에 기록
  const id = crypto.randomUUID()
  await redis.set(`ball:${id}`, {
    id, name, creatorName, imageUrl: blob.url,
    createdAt: Date.now(), approved: false,
  })
  await redis.lpush('ball:ids', id) // 전체 id 목록도 따로 관리

  return Response.json({ id, status: 'pending' })
}
```

### 프론트엔드에서 업로드 호출하는 모양

```ts
const formData = new FormData()
formData.append('image', croppedImageBlob) // canvas로 정사각형 크롭한 결과
formData.append('name', ballName)
formData.append('creatorName', creatorName)

const res = await fetch('/api/balls', { method: 'POST', body: formData })
const result = await res.json()
```

이건 "이런 모양이 되겠구나" 감을 잡는 용도야. 실제로 만들 땐 Vercel/Upstash 최신 문서를 같이 보면서 패키지 이름이나 함수 시그니처를 확인하는 게 안전해 (특히 `@upstash/redis`는 Vercel KV를 대체한 지 얼마 안 됐어서 문서가 계속 업데이트되고 있어).

---

## 8. 짚고 가면 좋은 주의사항

- **파일 크기/타입 제한**: 서버에서 반드시 검증해야 해. 브라우저 쪽 검증만 믿으면 안 돼 (누구나 브라우저를 우회해서 직접 요청을 보낼 수 있어).
- **업로드 속도 제한(rate limiting)**: 짧은 시간에 너무 많이 업로드하는 걸 막는 기능. 처음엔 없어도 되지만, 공개되면 스팸 방지를 위해 나중에 꼭 추가하는 게 좋아.
- **무료 티어 한도**: Blob 1GB/월, 전송량 10GB/월이 넘으면 요금이 발생하니, 서비스가 커지면 한 번씩 사용량을 확인하는 습관을 들이면 좋아.

---

이 문서는 "왜 이 구조인지"를 이해하기 위한 플랜이야. 실제로 손 대볼 준비가 되면, 위의 6번 순서대로 하나씩 같이 만들어보자 — 진행하고 싶은 단계를 말해주면 그 부분부터 자세히 도와줄게.
